if (CATKIN_ENABLE_TESTING)
	find_package(rostest REQUIRED)
  #catkin_add_gtest(test_kill_switch test_kill_switch.cpp)
  add_rostest_gtest(test_kill_switch launch/kill_switch.test test_kill_switch.cpp)
  cs_add_library(switch_emulator library/switch_emulator.cpp)
  include_directories(include)
  target_link_libraries(switch_emulator kill_switch_library)
  target_link_libraries(test_kill_switch switch_emulator)
  SET_TARGET_PROPERTIES(test_kill_switch PROPERTIES COMPILE_FLAGS "-std=c++11")
endif()